name: Build

on:
  push:
    branches: [main]
  pull_request:

jobs:
  macOS:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Swift version
        run: swift --version
      - name: Release build
        run: swift build -c release --disable-sandbox
